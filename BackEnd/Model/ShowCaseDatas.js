import mongoose from "mongoose";

const ShowcaseDatas = new mongoose.Schema({

    _id:{
        type:Number,
        bookId:Number
    },
    id:{
        type:Number,
        bookId:Number
    },
    Bid:{}

})